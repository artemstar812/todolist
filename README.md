A simple web-based to-do list application developed as a college coursework project. Allows user to create, edit and manage daily tasks. 
Built with HTML/CSS/JS and ASP.NET Core.

## 🚀 Running the Project

The application is designed to run locally.

1. Clone the repository
2. Run the backend:
   dotnet run
3. Open the frontend in your browser
